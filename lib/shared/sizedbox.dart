import 'package:flutter/material.dart';

class SizedBox5 extends StatelessWidget {
  const SizedBox5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.01);
  }
}

class SizedBox10 extends StatelessWidget {
  const SizedBox10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.02);
  }
}

class SizedBox20 extends StatelessWidget {
  const SizedBox20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.03);
  }
}

class SizedBox25 extends StatelessWidget {
  const SizedBox25({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.04);
  }
}

class SizedBox30 extends StatelessWidget {
  const SizedBox30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.05);
  }
}

class SizedBox40 extends StatelessWidget {
  const SizedBox40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.06);
  }
}

class SizedBox50 extends StatelessWidget {
  const SizedBox50({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.07);
  }
}

class SizedBox60 extends StatelessWidget {
  const SizedBox60({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.075);
  }
}

class SizedBox80 extends StatelessWidget {
  const SizedBox80({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.08);
  }
}

class SizedBox100 extends StatelessWidget {
  const SizedBox100({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: MediaQuery.of(context).size.height * 0.1);
  }
}

class SizedBoxWidth10 extends StatelessWidget {
  const SizedBoxWidth10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(width: MediaQuery.of(context).size.width * 0.08);
  }
}
